<?php
use Illuminate\Database\Migrations\Migration;use Illuminate\Database\Schema\Blueprint;use Illuminate\Support\Facades\Schema;
return new class extends Migration{public function up():void{if(!Schema::hasTable('trip_expenses'))Schema::create('trip_expenses',function(Blueprint $t){$t->id();$t->foreignId('employee_trip_id')->constrained('employee_trips')->cascadeOnDelete();$t->string('expense_type');$t->date('expense_date');$t->decimal('amount',14,2);$t->string('currency',3)->default('BDT');$t->text('description')->nullable();$t->string('receipt_path')->nullable();$t->boolean('reimbursable')->default(true);$t->enum('status',['Pending','Approved','Paid','Rejected'])->default('Pending');$t->timestamps();});}public function down():void{Schema::dropIfExists('trip_expenses');}};
