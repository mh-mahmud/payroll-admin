<?php
use Illuminate\Database\Migrations\Migration;use Illuminate\Database\Schema\Blueprint;use Illuminate\Support\Facades\Schema;
return new class extends Migration{public function up():void{if(!Schema::hasTable('employee_trips'))Schema::create('employee_trips',function(Blueprint $t){$t->id();$t->foreignId('employee_id')->constrained('employees')->cascadeOnDelete();$t->string('purpose');$t->string('destination');$t->date('start_date');$t->date('end_date');$t->text('description')->nullable();$t->text('expected_outcomes')->nullable();$t->string('document_path')->nullable();$t->decimal('advance_amount',14,2)->nullable();$t->enum('advance_status',['Requested','Approved','Paid','Reconciled'])->nullable();$t->decimal('expense_amount',14,2)->nullable();$t->enum('expense_status',['Pending','Approved','Paid','Rejected'])->nullable();$t->enum('status',['Planned','Ongoing','Completed','Cancelled'])->default('Planned');$t->timestamps();});}public function down():void{Schema::dropIfExists('employee_trips');}};
